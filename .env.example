# Path to the persistent data directory on the host
CLAUDE_WORKS_DATA=/mnt/user/appdata/claude-works

# Deploy guard token — generate with: openssl rand -hex 32
# Required only if using the deploy-guard service
DEPLOY_TOKEN=
