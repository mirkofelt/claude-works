services:
  claude-works:
    build: .
    container_name: claude-works
    restart: unless-stopped
    volumes:
      - claude-works-data:/data
    ports:
      - "8080:8080"
    environment:
      - LOG_LEVEL=INFO

volumes:
  claude-works-data:
