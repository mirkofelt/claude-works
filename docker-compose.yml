services:
  claude-works:
    build: .
    container_name: claude-works
    restart: unless-stopped
    volumes:
      - ${CLAUDE_WORKS_DATA:-./data}:/data
    ports:
      - "8080:8080"
    environment:
      - LOG_LEVEL=INFO
      - CLAUDE_HOME=/data/.claude
